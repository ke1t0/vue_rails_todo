<%= link_to '追加', new_todo_path %></>
<% @todos.each do |todo| %>
  <p>
    <%= todo.title %>
    <%= todo.expired_at&.to_s %>
  </p>

  <p>
    <%= link_to '変更', edit_todo_path(todo) %>
    <%= link_to '削除', todo_path(todo), method: :delete,data: {confirm: '本当に削除しますか？'} %>
  </p>
<% end %>